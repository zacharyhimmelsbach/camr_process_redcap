% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targets.R
\name{write_redcap_targets}
\alias{write_redcap_targets}
\title{Write a targets pipeline for a REDCap project}
\usage{
write_redcap_targets(
  redcap_url,
  api_token,
  out_dir,
  overwrite = FALSE,
  write_renviron = TRUE,
  init_git = TRUE,
  init_commit = TRUE
)
}
\arguments{
\item{redcap_url}{Character. The REDCap API endpoint URL
(e.g., "https://redcap.example.edu/api/").}

\item{api_token}{Character. A valid REDCap API token. Used during setup to
fetch project structure; stored in \code{.Renviron} for pipeline execution.}

\item{out_dir}{Character. Path to a new directory where pipeline files will
be created. Must not already exist.}

\item{overwrite}{Logical. Whether to overwrite existing files if \code{out_dir}
contains previous pipeline files. Defaults to FALSE.}

\item{write_renviron}{Logical. Whether to create a \code{.Renviron} file containing
the API token. Defaults to TRUE. The file is automatically added to \code{.gitignore}.}

\item{init_git}{Logical. Whether to initialize a git repository in \code{out_dir}.
Defaults to TRUE.}

\item{init_commit}{Logical. Whether to create an initial git commit.
Defaults to TRUE. Requires git user.name and user.email to be configured.}
}
\value{
Character vector of paths to the files that were created:
\itemize{
\item \code{camr_redcap_config.yml} - Configuration file
\item \verb{_targets.R} - The targets pipeline definition
\item \code{camr_report.qmd} - Quarto report template
\item \code{README.md} - Project documentation
\item \code{.Renviron} - API token (if \code{write_renviron = TRUE})
\item \code{renviron.example.txt} - Example .Renviron template
\item \code{.gitignore} - Git ignore rules
}
}
\description{
The main entry point for setting up a new REDCap data processing pipeline.
Connects to REDCap to fetch project metadata, then generates a complete
targets pipeline with configuration files, report template, and documentation.
}
\details{
After running this function:
\enumerate{
\item Review and customize \code{camr_redcap_config.yml}, especially the
\code{status_active_values} and \code{status_dropout_values} to match your
project's status field choices.
\item Run \code{targets::tar_make()} to execute the pipeline.
\item The pipeline will generate subject-level and visit-level datasets,
plus an HTML report with CONSORT diagrams.
}
}
\examples{
\dontrun{
# Create a new pipeline
write_redcap_targets(
  redcap_url = "https://redcap.example.edu/api/",
  api_token = Sys.getenv("REDCAP_API_TOKEN"),
  out_dir = "my_study_pipeline"
)

# Then in the new directory:
# 1. Edit camr_redcap_config.yml
# 2. Run targets::tar_make()
}

}
\seealso{
\code{\link{camr_redcap_metadata}}, \code{\link{camr_validate_config}}
}
