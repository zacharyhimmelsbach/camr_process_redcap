% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redcap_api.R
\name{camr_redcap_read}
\alias{camr_redcap_read}
\title{Export REDCap records in raw form}
\usage{
camr_redcap_read(redcap_url, api_token, fields = NULL, records = NULL)
}
\arguments{
\item{redcap_url}{Character. The REDCap API endpoint URL
(e.g., "https://redcap.example.edu/api/").}

\item{api_token}{Character. A valid REDCap API token with export permissions.}

\item{fields}{Character vector. Optional subset of field names to export.
If NULL (default), all fields are exported.}

\item{records}{Character vector. Optional subset of record IDs to export.
If NULL (default), all records are exported.}
}
\value{
A tibble containing the requested records with raw coded values.
For longitudinal projects, includes \code{redcap_event_name}. For projects
with repeating instruments, includes \code{redcap_repeat_instrument} and
\code{redcap_repeat_instance}.
}
\description{
Exports records from a REDCap project with raw (coded) values. This is the
primary function for retrieving study data from REDCap.
}
\examples{
\dontrun{
# Export all data
data <- camr_redcap_read(
  redcap_url = "https://redcap.example.edu/api/",
  api_token = Sys.getenv("REDCAP_API_TOKEN")
)

# Export specific fields
data <- camr_redcap_read(
  redcap_url = "https://redcap.example.edu/api/",
  api_token = Sys.getenv("REDCAP_API_TOKEN"),
  fields = c("record_id", "age", "gender")
)
}

}
\seealso{
\code{\link{camr_redcap_metadata}}, \code{\link{camr_apply_labels}}
}
