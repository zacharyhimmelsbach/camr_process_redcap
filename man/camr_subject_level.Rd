% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_processing.R
\name{camr_subject_level}
\alias{camr_subject_level}
\title{Create subject-level data}
\usage{
camr_subject_level(
  data,
  metadata,
  events,
  form_event_map = NULL,
  config,
  apply_labels = TRUE
)
}
\arguments{
\item{data}{A tibble of REDCap records as returned by
\code{\link{camr_redcap_read}}.}

\item{metadata}{A tibble of REDCap metadata as returned by
\code{\link{camr_redcap_metadata}}.}

\item{events}{A tibble of event metadata from \code{\link{camr_redcap_events}}.}

\item{form_event_map}{A tibble from \code{\link{camr_redcap_form_event_mapping}}.
Optional; if provided, used to identify single-event (subject-level) forms.}

\item{config}{A list containing configuration options including \code{id_field},
\code{status_field}, and optionally \code{subject_level_event}.}

\item{apply_labels}{Logical. Whether to convert multiple choice fields to
labeled factors. Defaults to TRUE.}
}
\value{
A tibble with one row per unique subject containing:
\itemize{
\item Subject identifiers
\item Fields from subject-level forms (or first event)
\item Latest status value across all events
}
}
\description{
Aggregates REDCap data to one row per subject, extracting baseline/enrollment
characteristics. For longitudinal projects, identifies subject-level forms
(those appearing at only one event) and extracts the first non-missing value
for each field.
}
\details{
The function uses this logic to identify subject-level data:
\enumerate{
\item If \code{form_event_map} is provided, forms appearing at only one event
are treated as subject-level forms.
\item Otherwise, if \code{config$subject_level_event} is set, that event is used.
\item Otherwise, the first event (by event_number) is used.
}
}
\examples{
\dontrun{
subject_data <- camr_subject_level(
  data = redcap_data,
  metadata = redcap_metadata,
  events = redcap_events,
  form_event_map = form_event_map,
  config = config
)
}

}
\seealso{
\code{\link{camr_subject_visit}}, \code{\link{camr_consort_data}}
}
